datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Play {
  id             Int      @id @default(autoincrement())
  createdAt      DateTime @default(now())
  correctAnswers Int
  score          Int
}
